import React, { Fragment } from "react";
import WorkSlider from "../components/slider/works-slider/WorkSlider";

const Home = () => {
  return (
    <Fragment>
      <WorkSlider />
    </Fragment>
  );
};

export default Home;
